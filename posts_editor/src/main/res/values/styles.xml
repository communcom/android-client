<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorPrimaryDark</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>


    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />


    <style name="WysiwygEditText">
        <item name="android:lineSpacingMultiplier">1.3</item>
        <item name="android:textColor">#000</item>
    </style>

    <style name="Description" parent="WysiwygEditText">
        <item name="android:textColor">@color/darkText</item>

        <item name="android:maxLength">120</item>
        <item name="android:maxLines">1</item>
        <item name="android:lines">1</item>
        <item name="android:singleLine">true</item>
        <item name="android:inputType">text</item>

        <item name="android:focusable">true</item>
        <item name="android:focusableInTouchMode">true</item>

        <item name="android:textSize">@dimen/image_description_text</item>
    </style>

    <!-- Theme for dialogs on Main screen -->
    <style name="App.Activity.Main.Dialog.Theme" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="colorAccent">@color/colorPrimary</item>  <!--Buttons-->
        <item name="android:textColorPrimary">@color/darkerText</item> <!--Text-->
    </style>
</resources>
