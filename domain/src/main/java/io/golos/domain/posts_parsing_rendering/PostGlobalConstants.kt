package io.golos.domain.posts_parsing_rendering

import io.golos.domain.posts_parsing_rendering.post_metadata.post_dto.DocumentFormatVersion

object PostGlobalConstants {
    val postFormatVersion =
        DocumentFormatVersion(1, 1)
}