package io.golos.cyber_android.ui.shared.utils

import io.golos.cyber_android.BuildConfig

object IntentConstants {
    const val ACTION_OPEN_NOTIFICATION = "${BuildConfig.APPLICATION_ID}.OPEN_NOTIFICATION"

    const val POST_CONTENT_ID = "${BuildConfig.APPLICATION_ID}.POST_CONTENT_ID"
    const val USER_ID = "${BuildConfig.APPLICATION_ID}.USER_ID"
    const val WALLET = "${BuildConfig.APPLICATION_ID}.WALLET"
}